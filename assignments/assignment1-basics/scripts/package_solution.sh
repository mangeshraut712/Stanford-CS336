#!/usr/bin/env bash
# Optional: course submission zip only (not used for this learning repo).
# Prefer keeping code in git; run only if you need the official Gradescope bundle.
set -euo pipefail

echo "This repo is a self-study project. Submission zips are not built by default."
echo "Use git push instead. To build a course zip anyway: see make_submission.sh"
exit 0
